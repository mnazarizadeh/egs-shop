package com.egs.shop.model.constant;

public interface ValidationMessages {

    String PasswordMatches = "Passwords don't match";
    String PasswordPattern = "Password should contains at least a small letter, a capital letter, a number, and a special character. Password length must be at least 8 characters";
    String UsernamePattern = "Username contains forbidden characters";
}
